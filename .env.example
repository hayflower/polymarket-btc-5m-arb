# =============================================================================
# Polymarket Crypto Up/Down Arbitrage Bot Configuration
# =============================================================================
# Copy this to .env and fill in your values

# Your Polygon wallet private key (for signing CLOB orders)
# Fund this wallet with USDC on Polygon and deposit to Polymarket
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# --- Market Selection ---
# Comma-separated assets to scan (btc, eth, sol, doge, avax, link)
ENABLED_ASSETS=btc
# Comma-separated durations in minutes (5, 15, 60)
MARKET_DURATIONS=5

# --- Strategy Parameters ---
# Maximum combined cost of Up + Down (below this = guaranteed profit)
MAX_COMBINED_COST=0.97
# Minimum net profit margin after fees (reject thinner arbs)
MIN_PROFIT_MARGIN=0.02
# USDC budget per window
ORDER_SIZE_USDC=50
# Max total position per window (USDC)
MAX_POSITION_PER_WINDOW=500
# Directional bias: 0.0=pure arb, +0.3=bias Up, -0.3=bias Down
DIRECTIONAL_BIAS=0.0
# Total bankroll in USDC (bot tracks deployed vs available)
BANKROLL=500

# --- Execution Parameters ---
# Number of slices to scale entry (gabagool22 uses 15-30)
NUM_SLICES=10
# Delay between slices in seconds
SLICE_DELAY_SEC=1.5
# Seconds into window before entering (gabagool22 enters ~120s in)
# Scaled proportionally for longer windows (e.g. 240s for 15m, 480s for 1h)
ENTRY_DELAY_SEC=120
# Order book poll interval in seconds
POLL_INTERVAL_SEC=1.0
# Re-read order book every N slices during execution (live re-pricing)
REPRICE_INTERVAL=3

# --- Order Mode ---
# "maker" = place below ask, 0% fee + daily USDC rebates (recommended)
# "taker" = buy at ask, immediate fill but pays taker fees
ORDER_MODE=maker
# Ticks below best ask for maker order placement (1-3 recommended)
MAKER_OFFSET_TICKS=1
# Seconds to wait for maker order fills before cancelling
MAKER_FILL_TIMEOUT_SEC=5.0

# --- Spending Controls ---
# Maximum daily capital deployment (USDC)
MAX_DAILY_SPEND=5000
# Stop trading if daily realized losses exceed this (USDC)
DAILY_LOSS_LIMIT=50

# --- Fee Configuration ---
# Fallback fee rate in basis points (fees are fetched live from CLOB per token).
# Only used if the CLOB /fee-rate endpoint is unavailable.
# Common values: 0 (fee-free markets), 1000 (15-min crypto markets)
FEE_RATE_BPS=0
